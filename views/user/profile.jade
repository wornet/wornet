extends ../layouts/wornet

block body
	p(style="margin-top: 50px;")
		a(href="/user/logout")
			=s("Déconnexion")

	+form#profile-photo("", "POST")(ng-submit="submit(user)" ng-controller="ProfilePhotoCtrl")
		img(src=user.photo alt=user.name.full)
		input.hidden(type="file" title=s("Votre photo") ng-model="user.photo" name="photo")
