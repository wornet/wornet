div(ng-repeat="status in recentStatus")
	.date(data-date="{{ status.date }}")
	.status-author
		img(width="25" height="25" ng-src="{{ status.author.thumb50 }}")
		| {{ status.author.name.full }}
	.status-content
		| {{ status.content }}