.well.loading.loading-big-height.fit-to-bottom.macro.all-status-block(ng-controller="StatusCtrl" ng-init="userConnected=" + !!user + ";myPublicInfos=" + JSON.stringify(myPublicInfos))
    .cb
    if isMe || (isAFriend && profile.allowFriendPostOnMe)
        include ../includes/status-form
    include ../includes/status-list
